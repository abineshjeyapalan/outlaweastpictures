{
  "name": "outlaw-east",
  "private": true,
  "version": "1.0.0",
  "description": "Outlaw East studio site — static frontend + a small Vercel serverless API for shared admin content.",
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  }
}
